이 프롬프트는 현재 단계가 아니라 향후 통합 단계에서만 사용한다.

목표:
drain-sensor-simulator의 센서값 화면에 YOLO 이미지 결과와 XGBoost 최종 판단 패널을 통합한다.

전제:
- 가상 센서 시뮬레이터가 안정적으로 동작한다.
- YOLO 모델 경로와 입력 이미지 처리 방식이 정리되어 있다.
- XGBoost 모델 입력 feature가 확정되어 있다.

주의:
현재 단계에서 이 작업을 수행하지 말 것.
